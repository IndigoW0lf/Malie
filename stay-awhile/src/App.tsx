/**
 * App root — the single indirection point. Points at Mālie, the cozy ahupuaʻa
 * tending game. The kit's blank starter still lives in src/starter/* for
 * reference; everything game-specific lives in src/malie/* above the kit's
 * src/services/* SDK layer.
 */
export { default } from './malie/MalieApp';
